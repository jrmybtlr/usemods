# Cleanup timeFrom / timeDifference playground layouts

- [ ] Fix `ExampleInputs` so `class` overrides actually replace the default row flex (not merge with `items-end`)
- [ ] Restack `TimeFrom.vue` into clean 2-row layout
- [ ] Restack `TimeDifference.vue` into clean rows; shorten Max parts / Style labels so they don't blow the row height
- [ ] Spot-check both look consistent

## Review

(pending)
